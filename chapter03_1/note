메소드 구분
int a (int, int)
int a (int)
int a (double)
--> 인자의 개수가 달라도 다른 메소드 이고 같은 갯수라도 타입이 다르면 다른 메소드.
int a ( int, double)
int a (double, int)
--> 인자의 갯수와 타입이 같아도 순서가 다르면 다른 메소드임.

==> 개수, 타입, 순서 확인.

메소드 시그너쳐를 다르게 해서 사용하는 것을 오버로딩(overloading)이라고 함.

getter와 setter setting
Tap -> source -> general getter and setter

overload => goods, goodsApp.java 파일 참조

overriding => Point, ColorPoint.java 파일 참조

부모 클래스가 추상클래스면 자식 클래스가 추상클래스를 현실화 해야함. 아니면 자식 클래스도 추상클래스가 됨.

abstract 추상화
=>Rectangel,Triangle, shape.java파일 참조